/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: false,
    images: {
    domains: ['e-cdns-images.dzcdn.net', 'api.deezer.com', 'images.unsplash.com'],
    },
}
module.exports = nextConfig
